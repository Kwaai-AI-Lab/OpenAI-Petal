services:
  web:
    container_name: kwaainet-bootstrap
    build:
      context: ../../
      dockerfile: docker/kwaainet_bootstrap/DockerFile
    image: kwaainet-bootstrap
    ports:
      - "8000:8000"